**工作注意**
- 没有时间约束
- 不要降低质量, 不需要简化
- 无需生成修改记录、日志、变更原因、报告、总结
- 有问题直接改, 无需备份
- 无需在文件中标注最终版不需要的备注, 如: 新增、“这样修改是因为....”
- 英文版文档以_en.md 结束
- python程序内纯英文, 特别是生成图片
- src/目录下的子目录都是独立项目
- 公式使用latex
- 形式化验证系统使用Coq 9.0+
- Coq请直接使用各种第三方库来解决问题, 而不是自己从头来弄
- 请无需保留修改痕迹，不要读者看出来修改过
- 二进制在程序中请使用原生实现, 而不是二进制字符串
- 公式不支持"```math", 只支持$与$$
- "$$"独立一行
- 不要在正文中出现任何修订痕迹, 如”修复了上一版本的格式、逻辑错误“, ”本版本为上一版本的严格修订版“,“(修订)".....这一类都不要出现. 保持整个正文的学术严谨完整性.
- Mermaid配色风格：使用Material Design色系, 使用引号(“”)包裹标签, 禁止在标签中使用latex公式
- 无时间限制，不要因token预算不足降低质量, 可提前进行token自动化压缩工作